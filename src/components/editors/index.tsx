import React, { ReactElement } from "react";

const Index = ({}: any, { Nav }: any) => {
  return ({}: any): ReactElement => {
    return (
      <div>
 
      </div>
    );
  };
};

export default Index;
